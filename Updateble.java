public interface Updateble 
    {
    //Esta interface é designada para os objetos que precisam ser atualizados a cada ciclo do jogo, ou seja, a cada "frame".

    //Declaração do método de atualização, que deve ser implementado por todas as classes que implementarem a interface Updateble.
    void update();
    }
